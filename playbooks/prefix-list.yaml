---
- name: Configurar prefix-lists dos routers
  hosts: all
  gather_facts: false

  tasks:
    - name: Mostrar prefix-lists atribuídas a este device
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }} usará: {{ device_prefix_lists[inventory_hostname] }}"

    - name: Placeholder — push real entra no módulo deploy
      ansible.builtin.debug:
        msg: "Aqui rodaria cisco.ios.ios_prefix_lists"